# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# Auto-refresh settings (optional - can be changed in UI)
DEFAULT_AUTO_REFRESH=false
DEFAULT_REFRESH_INTERVAL=60

# Dashboard Authentication (IMPORTANT for security!)
# Generate a password hash using: python3 generate_password_hash.py
# Leave empty to disable authentication (NOT recommended for production)
# Example hash (password: "admin123" - DO NOT USE IN PRODUCTION):
# DASHBOARD_PASSWORD_HASH=$2b$12$abcd1234...
DASHBOARD_PASSWORD_HASH=
